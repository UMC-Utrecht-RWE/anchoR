% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_anchor_result.R
\name{get_count_anchored_variables}
\alias{get_count_anchored_variables}
\title{Count anchored results by study variable}
\usage{
get_count_anchored_variables(
  anchor_hive_path,
  include_concept_counts = FALSE,
  concepts = NULL,
  variable_concepts = NULL
)
}
\arguments{
\item{anchor_hive_path}{Path to the anchored-variable parquet hive.}

\item{include_concept_counts}{Logical; whether to add counts from \code{concepts}.}

\item{concepts}{Concept records to count when
\code{include_concept_counts = TRUE}. See \code{\link[=anchor]{anchor()}} for accepted source types.}

\item{variable_concepts}{A data frame containing \code{variable_id} and
\code{concept_id}, required only when \code{include_concept_counts = TRUE}.}
}
\value{
A data.table with \code{variable_id} and \code{n_anchored}. When concept
counts are requested, \code{concept_id} and \code{n_concept} are also returned.
}
\description{
Counts persisted rows in an anchored-variable parquet hive without
materializing those rows in R. The variable identifiers are discovered from
the completed hive; variables for which no result partition was written
cannot be inferred and are not returned.
}
\details{
When \code{include_concept_counts = TRUE}, the function also counts rows in the
supplied \code{concepts} source. Because anchored output does not retain
\code{concept_id}, \code{variable_concepts} supplies the relationship between each
result variable and its source concept. \code{concepts} accepts the same inputs
as \code{\link[=anchor]{anchor()}}: an in-memory data frame, a DuckDB database path, or one or
more parquet files, directories, or globs.

The anchored count is the number of sparse result rows written by
\code{\link[=anchor]{anchor()}}, not necessarily the number of \code{TRUE} values. For example, rows
produced by \code{LATEST}, \code{COUNT}, and \code{ALL} selectors are counted as anchored
rows too.
}
