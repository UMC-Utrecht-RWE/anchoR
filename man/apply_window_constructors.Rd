% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/define_window.R
\name{apply_window_constructors}
\alias{apply_window_constructors}
\title{Apply Window Constructors to a Cross-Joined Frame}
\usage{
apply_window_constructors(window_dt, constructor_env)
}
\arguments{
\item{window_dt}{A data.table with a \code{constructor} column, such as one
produced by \code{cross_join_population_metadata()}.}

\item{constructor_env}{Environment used to resolve user-defined
constructors. See \code{resolve_window_constructor()}.}
}
\value{
A new data.table combining every constructor's output, each row
carrying \code{window_start}/\code{window_end}.
}
\description{
Runs one constructor per unique \code{constructor} value in \code{window_dt} and
combines their outputs. A constructor may return a different number of
rows than it was given, an event-based constructor (see
\code{R/pregnancy_window.R}) turns one input row into zero, one, or many
candidate windows, so outputs are combined by row-binding rather than
assigned back into fixed row positions.
}
\keyword{internal}
