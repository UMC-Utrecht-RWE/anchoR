% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pregnancy_window.R
\name{event_window_engine}
\alias{event_window_engine}
\title{event-Based Window Engine}
\usage{
event_window_engine(window_dt, event_select, end_boundary = "event_END")
}
\arguments{
\item{window_dt}{A data.table produced by \code{cross_join_population_metadata()}.
When \code{event_select} is \code{"PRIOR"}, its optional \code{start_look_back}/
\code{end_look_back} columns (NA by default) additionally restrict which
prior episodes are eligible at all: only episodes overlapping the
anchor-relative range \code{anchor + start_look_back} through
\code{anchor + end_look_back} are considered; the survivors' windows are
still computed from \code{start_offset}/\code{end_offset} as usual, unclipped.}

\item{event_select}{One of \code{"PRIOR"}, \code{"CURRENT"}, \code{"OUTSIDE_ALL"}.}

\item{end_boundary}{One of \code{"event_END"} or \code{"ANCHOR"}; only used when
\code{event_select} is \code{"CURRENT"}.}
}
\value{
A data.table with the same columns as \code{window_dt} (minus the
\code{.window_row_id}-preserving columns, which are carried through
unchanged) plus \code{window_start}/\code{window_end}.
}
\description{
Shared engine behind every event-based constructor (\code{IN_PRIOR_PREG},
\code{SINCE_START_CURRENT_PREG}, \code{ANYTIME_CURRENT_PREG}, \code{OUTSIDE_ALL_PREG}).
Each \code{window_dt} row references, via its \code{event_col} value, a
population list-column holding that person's events (a data.table with
\code{event_start}/\code{event_end} columns, one row per event). One input
row can expand into zero, one, or many output rows -- one per candidate
window.
}
\keyword{internal}
