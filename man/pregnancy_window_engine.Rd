% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pregnancy_window.R
\name{pregnancy_window_engine}
\alias{pregnancy_window_engine}
\title{Episode-Based Window Engine}
\usage{
pregnancy_window_engine(window_dt, episode_select)
}
\arguments{
\item{window_dt}{A data.table produced by \code{cross_join_population_metadata()},
with \code{.episodes} already nested onto it.}

\item{episode_select}{One of \code{"CURRENT"}, \code{"PRIOR"}, \code{"CURRENT_AND_PRIOR"},
\code{"OUTSIDE_ALL"}.}
}
\value{
A data.table with the same columns as \code{window_dt} plus
\code{window_start}/\code{window_end}.
}
\description{
Shared engine behind every episode-based constructor.
Each \code{window_dt} row carries that person's episodes in the \code{.episodes}
list-column (a data.table with \code{start_episode}/\code{end_episode} columns,
one row per episode), added by \code{nest_episodes_onto_population()}. One
input row can expand into zero, one, or many output rows, one per
candidate window.
}
\details{
For \code{"CURRENT"}/\code{"PRIOR"}/\code{"CURRENT_AND_PRIOR"}, episodes are first
classified relative to the anchor (see \code{classify_episodes()}), then each
selected episode independently contributes its own window(s) via
\code{episode_windows()}, purely from its own \code{start_episode}/\code{end_episode}
and the row's four border offsets. \code{"OUTSIDE_ALL"} is different: it
finds the gaps \emph{between all} of a person's episodes inside
\verb{[anchor_start_col + anchor_start_offset, anchor_end_col + anchor_end_offset]} directly; the border offsets are not used.
Regardless of \code{episode_select}, every candidate window is then clipped
to that same \verb{[anchor_start_col + anchor_start_offset, anchor_end_col + anchor_end_offset]} boundary via \code{clip_to_anchor_bounds()} -- for
\code{"OUTSIDE_ALL"} this is a no-op (the gaps are already computed inside
that range), for the other three it's what makes \code{anchor_start_offset}/
\code{anchor_end_offset} a hard, constructor-independent time boundary.
}
\keyword{internal}
