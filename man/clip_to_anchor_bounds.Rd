% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pregnancy_window.R
\name{clip_to_anchor_bounds}
\alias{clip_to_anchor_bounds}
\title{Clip candidate windows to the project's hard anchor-relative boundary}
\usage{
clip_to_anchor_bounds(
  windows,
  anchor_start_val,
  anchor_start_offset,
  anchor_end_val,
  anchor_end_offset
)
}
\arguments{
\item{windows}{A data.table with \code{window_start}/\code{window_end} columns.}

\item{anchor_start_val, anchor_end_val}{The row's own anchor date(s)
(single Dates), from \code{anchor_start_col}/\code{anchor_end_col}.}

\item{anchor_start_offset, anchor_end_offset}{Single offsets (or \code{NA}).}
}
\value{
\code{windows}, with \code{window_start}/\code{window_end} clipped in place.
}
\description{
\code{anchor_start_offset}/\code{anchor_end_offset} define a hard boundary,
\verb{[anchor_start_val + anchor_start_offset, anchor_end_val + anchor_end_offset]}, that every episode-based window must fall inside,
regardless of constructor. Each side is applied independently and only
when not \code{NA} (the same convention the border-offset pairs use): a set
\code{anchor_start_offset} raises \code{window_start} up to at least the
boundary's lower edge; a set \code{anchor_end_offset} lowers \code{window_end}
down to at most the boundary's upper edge. A window entirely outside the
boundary comes out with \code{window_start > window_end}, left for
\code{finalize_windows()} to mark invalid, the same as any other empty
window; this function never drops rows itself.
}
\keyword{internal}
