% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tune_anchor_settings.R
\name{tune_anchor_settings}
\alias{tune_anchor_settings}
\title{Benchmark \code{anchor()} settings to find the fastest combination}
\usage{
tune_anchor_settings(
  population,
  metadata,
  concepts,
  episodes = NULL,
  anchor_col = "T0",
  by_values = c("whole", "variable", "selector"),
  chunk_size_values = c(5L, 10L, 20L, 50L),
  staging_mode_values = c("memory", "disk"),
  publish_values = c("once", "per_chunk"),
  repeats = 1L,
  prepare_con = NULL,
  benchmark_hive_root = tempdir()
)
}
\arguments{
\item{population}{A data frame containing the study population. Must include
a \code{person_id} column and the anchor date column specified by
\code{anchor_col}.}

\item{metadata}{A data frame describing the variables to anchor. Must contain
the columns required by \code{validate_anchor_inputs()}.}

\item{concepts}{A concept table as a data frame, a DuckDB file path whose
\code{concept_table} contains \code{person_id}, \code{concept_id}, and
\code{date}, or parquet file location(s).}

\item{episodes}{Optional data frame with \code{person_id},
\code{start_episode}, \code{end_episode} columns. Required when
\code{metadata} uses an episode-based constructor
(\code{in_current_pregnancy}, \code{in_prior_pregnancy},
\code{in_current_and_prior}, \code{outside_all_pregnancy}); see
\code{R/pregnancy_window.R}.}

\item{anchor_col}{Character. Name of the column in \code{population} to use
as the index date when metadata does not specify an anchor column.
Defaults to \code{"T0"}.}

\item{by_values}{Which \code{by} modes to benchmark. Defaults to all three.}

\item{chunk_size_values}{Chunk sizes to try when \code{by = "variable"}.
Defaults to \code{c(5L, 10L, 20L, 50L)}.}

\item{staging_mode_values}{Staging modes to try when \code{by = "variable"}.
Defaults to both \code{"memory"} and \code{"disk"}.}

\item{publish_values}{Publish modes to try when \code{by = "variable"}.
Defaults to both \code{"once"} and \code{"per_chunk"}.}

\item{repeats}{How many times to time each setting combination.
Combinations are ranked by the median of their successful runs; more
repeats give a steadier estimate at the cost of a longer benchmark.
Defaults to \code{1}.}

\item{prepare_con}{Optional function taking a single argument (the open
DBI connection selector queries run against).}

\item{benchmark_hive_root}{Existing directory below which each benchmark
run creates an isolated throwaway output hive. Defaults to \code{tempdir()}.
To benchmark production storage characteristics, supply a directory on
the same mounted filesystem as the production \code{anchor_hive_path}.}
}
\value{
A list:
\describe{
\item{\code{runs}}{One row per individual timed run: the setting
combination, \code{repeat_index}, \code{elapsed_secs}, \code{success}, and
\code{error_message} (\code{NA} on success).}
\item{\code{summary}}{One row per setting combination: the setting
combination, \code{median_elapsed_secs} (over successful runs only),
\code{n_success}, \code{n_failed}; ordered fastest first. A combination that
never succeeded sorts last, with \code{median_elapsed_secs} \code{NA}.}
\item{\code{best}}{The single fastest row of \code{summary} that succeeded at
least once, or \code{NULL} if every combination failed.}
}
}
\description{
Runs \code{anchor()} once (or \code{repeats} times) for every combination of \code{by},
\code{chunk_size}, \code{staging_mode}, and \code{publish} requested, against the given
\code{population}/\code{metadata}/\code{concepts} (and optional \code{episodes}), and times
each run. There's no single combination that's fastest everywhere, it
depends on things specific to a client's own environment: how
\code{anchor_hive_path} is mounted (local disk vs. slow/network storage), how
many variables and distinct selectors \code{metadata} has, and how big
\code{concepts} is. Meant to be run once, up front, against a representative
sample of a new client's own data and storage, so that expensive
production runs can go straight to whatever settings this found fastest
instead of guessing.
}
\details{
\code{by = "whole"} and \code{by = "selector"} don't use \code{chunk_size},
\code{staging_mode}, or \code{publish} (see \code{\link[=anchor]{anchor()}}), so each is benchmarked
once per repeat regardless of what those arguments are set to;
\code{by = "variable"} is benchmarked once per repeat per combination of the
three.

Every run writes to its own throwaway hive below \code{benchmark_hive_root},
removed again immediately afterward. Set \code{benchmark_hive_root} to a
directory on the same storage as the production hive when storage behavior
is relevant to the comparison. A run that errors (for example, a
\code{staging_mode} a client's storage doesn't support) is recorded as a failure
rather than stopping the whole benchmark, so one bad combination doesn't
prevent finding out about the rest.
}
