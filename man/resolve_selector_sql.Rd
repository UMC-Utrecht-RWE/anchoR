% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/define_selector.R
\name{resolve_selector_sql}
\alias{resolve_selector_sql}
\title{Resolve a Selector's SQL by Name}
\usage{
resolve_selector_sql(selector_name, selector_env)
}
\arguments{
\item{selector_name}{Value of the metadata \code{selector} column.}

\item{selector_env}{Environment searched for user-defined selectors.}
}
\value{
SQL text (a single string) for the selector.
}
\description{
Looks up the SQL text for a given \code{selector} value. Built-in selectors
(e.g. \code{RANGE_COUNT}) are always resolved from the bundled \code{inst/sql}
templates. A user-defined selector is found by name
(\verb{<selector>_selector}, lower-cased) in \code{selector_env}, so anyone can add
one with \code{make_selector()} without editing this package, the same
pattern \code{resolve_window_constructor()} uses for window constructors.
}
\keyword{internal}
