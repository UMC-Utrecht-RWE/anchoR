% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pregnancy_window.R
\name{outside_all_event_gaps}
\alias{outside_all_event_gaps}
\title{Compute the "outside all events" gap windows}
\usage{
outside_all_event_gaps(events, anchor, start_offset, end_offset)
}
\arguments{
\item{events}{A data.table with \code{event_start}/\code{event_end} columns,
one row per event for a single person.}

\item{anchor}{A single Date.}

\item{start_offset}{Integer offsets applied to \code{anchor} to get
the overall search range (order-independent; the smaller of the two
resulting dates is used as the range's lower bound).}

\item{end_offset}{Integer offsets applied to \code{anchor} to get
the overall search range (order-independent; the larger of the two
resulting dates is used as the range's upper bound).}
}
\value{
A data.table with \code{window_start}/\code{window_end} columns, one row
per gap (possibly zero rows).
}
\description{
Within \verb{[anchor + start_offset, anchor + end_offset]}, returns the parts
that do not fall inside any event. An event always fences the gaps
around it, even one that contains \code{anchor} itself.
}
\keyword{internal}
