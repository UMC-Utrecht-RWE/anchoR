# Get the Path to a Selector SQL Template

Get the Path to a Selector SQL Template

## Usage

``` r
selector_sql_path(selector)
```

## Arguments

- selector:

  Selector name such as `"LATEST"` or `"COUNT"`.

## Value

An absolute path to the SQL template.
