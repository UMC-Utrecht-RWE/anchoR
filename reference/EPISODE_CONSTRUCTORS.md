# Constructor names backed by the episode window engine

Used by
[`define_window()`](https://umc-utrecht-rwe.github.io/anchoR/reference/define_window.md)
to decide whether `episodes` must be supplied and nested onto
`population` before any constructor runs.

## Usage

``` r
EPISODE_CONSTRUCTORS
```
