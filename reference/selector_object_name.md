# Name a Custom Selector Would Be Looked Up Under

Name a Custom Selector Would Be Looked Up Under

## Usage

``` r
selector_object_name(selector_name)
```

## Arguments

- selector_name:

  Value of the metadata `selector` column.

## Value

The `<selector>_selector` object name, lower-cased.
