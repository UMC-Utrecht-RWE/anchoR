# Read a Built-in Selector's SQL Template

Read a Built-in Selector's SQL Template

## Usage

``` r
read_builtin_selector_sql(selector)
```

## Arguments

- selector:

  Selector name such as `"LATEST"` or `"COUNT"`.

## Value

SQL text (a single string) read from `inst/sql`.
